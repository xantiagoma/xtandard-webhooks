/**
 * Subpath entry for `@xtandard/webhooks/drizzle/sqlite`. Re-exports the SQLite
 * Drizzle table factory so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./drizzle/sqlite.ts";
