/**
 * Subpath entry for `@xtandard/webhooks/authorization/delegated`. Re-exports
 * the delegated authorization provider so the bundler can map it to its own
 * dist file.
 *
 * @module
 */

export * from "./authorization/delegated.ts";
