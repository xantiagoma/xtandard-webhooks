/**
 * Subpath entry for `@xtandard/webhooks/storage/sqlite`. Re-exports the
 * `bun:sqlite` storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/sqlite.ts";
