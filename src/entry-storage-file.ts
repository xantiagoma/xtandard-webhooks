/**
 * Subpath entry for `@xtandard/webhooks/storage/file`. Re-exports the
 * file-system storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/file.ts";
