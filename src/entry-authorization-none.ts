/**
 * Subpath entry for `@xtandard/webhooks/authorization/none`. Re-exports the
 * allow-all authorization provider so the bundler can map it to its own dist
 * file.
 *
 * @module
 */

export * from "./authorization/none.ts";
