/**
 * Subpath entry for `@xtandard/webhooks/elysia`. Re-exports the Elysia adapter
 * (plain panel + typed Eden plugin) so the bundler can map it to its own dist
 * file.
 *
 * @module
 */

export * from "./adapters/elysia.ts";
