/**
 * Subpath entry for `@xtandard/webhooks/storage/redis`. Re-exports the Redis
 * storage adapters so the bundler can map them to their own dist file.
 *
 * @module
 */

export * from "./storage/redis.ts";
