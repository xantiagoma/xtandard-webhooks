/**
 * Subpath entry for `@xtandard/webhooks/storage/cloudflare-kv`. Re-exports the
 * Cloudflare Workers KV storage adapter so the bundler can map it to its own
 * dist file.
 *
 * @module
 */

export * from "./storage/cloudflare-kv.ts";
