/**
 * Subpath entry for `@xtandard/webhooks/storage/libsql`. Re-exports the
 * libSQL/Turso storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/libsql.ts";
