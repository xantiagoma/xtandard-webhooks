/**
 * Subpath entry for `@xtandard/webhooks/storage/postgres`. Re-exports the
 * Postgres storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/postgres.ts";
