/**
 * Subpath entry for `@xtandard/webhooks/storage/unstorage`. Re-exports the
 * unstorage wrapper adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/unstorage.ts";
