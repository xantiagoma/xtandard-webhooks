/**
 * Subpath entry for `@xtandard/webhooks/storage/drizzle`. Re-exports the
 * Drizzle storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/drizzle.ts";
