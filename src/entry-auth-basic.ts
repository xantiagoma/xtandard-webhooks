/**
 * Subpath entry for `@xtandard/webhooks/auth/basic`. Re-exports the HTTP Basic
 * auth provider so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./auth/basic.ts";
