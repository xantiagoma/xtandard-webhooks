/**
 * Subpath entry for `@xtandard/webhooks/authorization/roles`. Re-exports the
 * role-based authorization provider so the bundler can map it to its own dist
 * file.
 *
 * @module
 */

export * from "./authorization/roles.ts";
