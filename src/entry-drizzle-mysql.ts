/**
 * Subpath entry for `@xtandard/webhooks/drizzle/mysql`. Re-exports the MySQL
 * Drizzle table factory so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./drizzle/mysql.ts";
