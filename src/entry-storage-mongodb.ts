/**
 * Subpath entry for `@xtandard/webhooks/storage/mongodb`. Re-exports the
 * MongoDB storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/mongodb.ts";
