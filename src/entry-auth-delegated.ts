/**
 * Subpath entry for `@xtandard/webhooks/auth/delegated`. Re-exports the
 * delegated auth provider so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./auth/delegated.ts";
