/**
 * Subpath entry for `@xtandard/webhooks/storage/memory`. Re-exports the
 * in-memory storage adapter so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./storage/memory.ts";
