/**
 * Subpath entry for `@xtandard/webhooks/drizzle/pg`. Re-exports the Postgres
 * Drizzle table factory so the bundler can map it to its own dist file.
 *
 * @module
 */

export * from "./drizzle/pg.ts";
